#include "main.h"

/**
 * _isupper - Entry point
 *
 *@c: character need identify
 *
 * Return: 1 if upper 0 otherwise
 */

int _isupper(int c)
{
	char ;

	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
